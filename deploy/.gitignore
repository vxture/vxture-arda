# Runtime env files contain production values and must stay local to the deploy host.
.env
.env.*
!.env.example
!.env.*.example

# Runtime secrets must not be committed.
secrets/*
!secrets/.gitkeep
!secrets/README.md
!secrets/*.example
