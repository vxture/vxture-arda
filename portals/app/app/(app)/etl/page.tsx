import { PlaceholderSection } from "../../ui/placeholder";

// Phase 1: real surface is /dashboard; these sections render the
// under-construction placeholder until their screens are built.
export default function Page() {
  return <PlaceholderSection />;
}
